# ==========================================
# RumorDetect
# ==========================================
# 安装方式: pip install -r requirements.txt

# ---------- 深度学习框架 ----------
torch>=1.13.0
torchvision>=0.14.0     # torch的依赖项

# ---------- 自然语言处理 ----------
transformers>=4.30.0     # BERT模型（HuggingFace）
sentence-transformers>=2.2.0  # 语义嵌入（用于RAG检索）

# ---------- 向量检索 ----------
faiss-cpu>=1.7.4         # 稠密向量检索（CPU版本）
# 如有GPU可用，可替换为: faiss-gpu>=1.7.4

# ---------- 网络请求 ----------
requests>=2.28.0         # LLM API调用

# ---------- 数据处理 ----------
numpy>=1.22.0
scikit-learn>=1.0.0      # 评估指标

# ---------- 工具 ----------
tqdm>=4.64.0             # 进度条
